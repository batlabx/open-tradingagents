"""MCP data-tool servers.

Each file is a standalone FastMCP server exposing one market-data capability
over stdio, using only free / keyless sources. Point any MCP client (this graph,
Claude Desktop, Cursor) at them and the tools work everywhere.
"""
